<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>

        {% block stylesheets %}
        <link rel="stylesheet"href="https://bootswatch.com/5/lux/bootstrap.min.css">
        {% endblock %}

        {% block javascripts %}
           
        {% endblock %}
    </head>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">mygym</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('app_client_index') }}">clients
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="{{('coaches')}}">coaches
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('app_membership_index') }}">Membership
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('app_membership_type_index') }}">Membership Types
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('app_private_coaching_index') }}">private coaching
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('app_planning_index') }}">Planning
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
      </ul>
     
    </div>
  </div>
</nav> 
        <body><div class="container"> 
        {% block body %}{% endblock %}
        </div>
    </body>
</html>
