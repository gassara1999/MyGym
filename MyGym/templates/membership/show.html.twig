{% extends 'base.html.twig' %}

{% block title %}Membership{% endblock %}

{% block body %}
    <h1>Membership</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ membership.id }}</td>
            </tr>
            <tr>
                <th>DateBegin</th>
                <td>{{ membership.dateBegin ? membership.dateBegin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateEnd</th>
                <td>{{ membership.dateEnd ? membership.dateEnd|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ membership.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_membership_index') }}">back to list</a>

    <a href="{{ path('app_membership_edit', {'id': membership.id}) }}">edit</a>

    {{ include('membership/_delete_form.html.twig') }}
{% endblock %}
